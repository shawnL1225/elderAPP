<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.elderapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ElderAPP">
        <activity android:name=".EditPlaceActivity"></activity>
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddEventActivity"
            android:label="@string/title_activity_add_event" />
        <activity
            android:name=".elder.ElderActivity"
            android:label="@string/title_activity_elder" />
        <activity
            android:name=".volunteer.VolunteerActivity"
            android:label="@string/title_activity_volunteer" />
        <activity
            android:name=".register.RegisterActivity"
            android:label="@string/title_activity_signup" />
        <activity android:name=".LoginActivity"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>